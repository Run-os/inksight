# 2026-07-13

## 完成：epd_42_rlcd_s3_n16r8 设备文档 + 项目运行机制/优化建议
- 通读 firmware/src 关键文件（config.h、epd_driver.cpp/h、rlcd_bsp.cpp/h、main.cpp、
  display.cpp、network.cpp、storage.cpp、portal.h、platformio.ini），梳理出
  `epd_42_rlcd_s3_n16r8`（ESP32-S3-RLCD-4.2 / ST7305 反射式 LCD）的设备适配与功能逻辑。
- 产出两份标准化开发文档（Markdown）：
  - `docs/device_epd_42_rlcd_s3_n16r8.md`：引脚/构建/驱动架构/ST7305 实现/渲染/电池/按键/
    深度睡眠/启动流程/主循环/配网/离线缓存/注意事项/新增同类面板套路。
  - `docs/inksight_firmware_optimization.md`：整体架构、启动与执行状态机、可复用 Patterns、
    已可落地优化、面向同类 ESP 项目的通用优化建议。
- 尝试导出到 ima 知识库 `esp32-rlcd-4.2`：确认 KB 存在（id=7482084985686315），
  但 ima-mcp 仅只读，无上传接口，**未能自动推送**；已交付 .md 文件供用户手动导入。
- 已将"ima-mcp 只读限制"写入 MEMORY.md 长期记忆。

## 完成：RLCD 专项重构（去休眠 + 启动页优先 + 精简代码）
三项重构全部落地并通过 PlatformIO 编译（env=epd_42_rlcd_s3_n16r8，SUCCESS）。
- 1) 移除所有屏幕休眠机制：main.cpp 全量重写，删除 enterDeepSleep/detectWakeupReason/
  WakeupReason/esp_sleep.h；epd_driver.cpp/h 删除 epdSleep。设备改为常驻在线（loop 连续运行，
  handleLiveMode/handleFailure 按 refreshIntervalMs 重试而非深度睡眠）。
- 2) 启动流程调整：main.cpp setup() 中 `showBootScreen()`（白底+"InkSight"/"Initializing..."）
  在 `epdInit` 之后、`connectWiFi`/网络请求之前渲染并 smartDisplay，再 delay(800)，确保通电
  先出启动页再跑业务。
- 3) 代码精简（仅保留 epd_42_rlcd_s3_n16r8 可用代码）：
  - epd_driver.cpp 由 ~1088 行(5 面板分支)重写为 RLCD-only ~90 行（删 SSD1683/WFT/GxEPD2 等
    全部分支 + 2bpp color 代码）；epd_driver.h 删 epdDisplay2bpp/epdSleep。
  - config.h 收敛为单板（BOARD_PROFILE_RLCD_S3 + #error 守卫），删 PIN_AI_CHAT_SW、
    AI_CHAT_BTN_HOLD_MS/VOCAB_*_HOLD_MS、AUTO_BOOT_AI_CHAT/VOCAB_REVIEW_BUILD、
    COLOR_BUF_LEN/colorBuf/useColorBuf/ensureColorBuf、多板引脚分支。
  - network.cpp 69.5KB→31.5KB：删全部 voice/vocab 函数(postVocabEvent…voiceWsClose)、
    gVoiceWs* 变量、encodeBase64/decodeBase64ToHeap/parseWsServerUrl/handleVoiceWsEvent、
    audio.h/WebSocketsClient.h 包含、`#if EPD_BPP>=2` 2bpp 拉取块。
    （注意：syncNTP/tickTime 原本在 voice 块之后，初版截断误删，已从 git 恢复补回。）
  - network.h 删 VoiceWsEvent/enum/struct、AudioChunkCallback、全部 voice/vocab 声明。
  - display.cpp 删 smartDisplay 内 2bpp 分支+GYE042A87 条件、AI-chat 绘图 helpers+showAiChatStatus、
    语音指示器(showVoiceIndicator/hideVoiceIndicator/showVoiceChatScreen+ROBOT_ICON)、
    updateVocabRatingRegion；display.h 同步删声明。
  - 删除文件：audio{.cpp,.h}、audio_codec{.cpp,.h}、audio_service{.cpp,.h}、epd_wft.h、epd4in2_wft.h。
  - platformio.ini 50+ env→仅 epd_42_rlcd_s3_n16r8；lib_deps 清空（GxEPD2/Adafruit/WebSockets 均不再被引用）。
- 构建结果：RAM 23.7%(77KB)，Flash 20.2%(1.06MB/5.24MB app)，firmware_merged.bin 已生成。
- 关键经验：所有 `#if EPD_BPP>=2` / `#if defined(EPD_PANEL_*)` 非 RLCD 分支在 RLCD env 下被预处理器
  排除（不影响编译），故面板/2bpp 清理是"源码整洁"而非"修编译"；而 network.cpp 的 voice 代码无宏守卫、
  总会编译，删除必须完整一致（audio.h/WebSockets 包含一并移除），否则会断编译。
- 已用 git show HEAD:firmware/src/network.cpp 恢复被误删的 syncNMP/tickTime（教训：大块截断前先确认
  块尾之后是否还有非目标函数）。

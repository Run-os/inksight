# 2026-07-13 工作日志

## 新项目核心功能规划（两大功能）
- 产出技术规划文档：`docs/core-features-plan.md`（v1，待评审）。
- 两大核心功能：① 原生待办列表（设备本地渲染）；② 网络图片展示（后端下发 1-bit BMP，本地缓存≤5张，按键切换）。
- 统一 UI：黑白墨水长屏复古像素风；三层结构（状态栏/内容区/底栏），字号层级 时间>待办正文>日期·提醒·分页·底栏。

## 已与用户确认的三项决策
- D1 待办来源：Web/App 后台管理，设备只拉取展示（设备无键盘/触控）。
- D2 图片后端：独立新建 FastAPI 服务 `inksight-content`（与现有 LLM backend 解耦）。
- D3 按键模型：短按 KEY(GPIO18)=上一页/上一张；短按 BOOT(GPIO0)=下一页/下一张。
  - 规划建议（待确认）：长按 KEY(≥2s)=在待办/图片间切 App；长按 BOOT(≥2s)=进配网 Portal（原长按 KEY 行为迁移）。
  - BOOT(GPIO0) 仅在"按住+重新上电"进下载模式，运行时长按可安全复用为功能键。

## 关键事实缺口（重要）
- 现有固件 `display.cpp::drawText` 仅内置 5×7 ASCII 字体，**无 CJK 字库**，无法渲染中文待办正文。
- 结论："待办原生渲染"必须在固件新增 CJK 16×16 点阵字库（建议 3500 常用字 ~112KB，可放 Flash/PSRAM）+ `draw7Seg` 数码管时间 + `drawMixed` 中英文混排。
- 图片模式：后端把图预处理成内容区尺寸(400×214)的 1-bit BMP 下发，设备原生画 chrome 并 blit 图片缓冲，本地缓存 5 张用 PSRAM。

## 字库原料与精确点阵格式（已探明，2026-07-13）
- 仓库已含可直接用作字库原料的中文矢量/位图字体，无需外找：
  - TTF（首选）：`backend/fonts/truetype/NotoSerifSC-Regular.ttf`（思源宋体，完整中文，可精确栅格到 16×16）。
  - PCF 位图（备选）：`backend/fonts/bitmap/wqy-zenhei-13.pcf`、`NotoSerifSC-*.pcf`（已含中文，但尺寸固定 9–13px，需裁剪/缩放凑 16×16）。
- 现有固件中文点阵精确格式（`display.cpp::drawGlyph16`，323 行）：每个字 = `const uint16_t glyph[16]`，16 行，每行一个 uint16_t；`bit15=最左列, bit0=最右列`，黑色=1（置位）；取位用 `(bits & (1<<(15-col)))`，与字节序无关。
- 生成脚本产物：`const uint16_t cjk16[N][16]`，与现有 drawGlyph16 直接兼容 → 固件绘制代码零改动，只需把写死的工期标签换成查表。原料用 TTF+Pillow(freetype)：渲染 16×16 灰度 → 阈值二值化(黑=1) → 打包 uint16_t → 输出 `.h`。

## 为何必须"预生成字库"——历史显示链路（已探明，2026-07-13）
- 原本项目中文显示有两条链路，设备端几乎不画中文，故烧录不需要字库脚本：
  1. **设备端只硬编码十几个"星期时段"字**：`display.cpp:310 periodGlyphs` 用 `GLYPH_SHEN/GUY_YE/...` 等写死的 `const uint16_t` 数组（深夜/凌晨/早晨/上午/中午/下午/傍晚/夜晚），非从 TTF 生成、非脚本产物。设备端"通用中文渲染"能力为零。
  2. **所有带中文画面由后端实时渲染 BMP 下发**：`backend/core/json_renderer.py:419/1444` 加载 `NotoSerifSC-Regular.ttf`，用 Pillow/freetype 把 JSON 内容渲染成 1-bit BMP，设备经 `/api/render` 拉取直接显示。后端有完整字体引擎，故"字库"活在后端运行时，无需预生成 C 数组、无需脚本。
- **结论**：原本"字库"在后端（运行时 TTF+freetype），不在设备端；设备只是 BMP 显示器 + 少量硬编码字，所以烧录无脚本。现在做"待办原生渲染"= 设备要自绘任意中文 → 首次需设备端通用字库 → MCU 无 freetype → 必须编译前用脚本把 TTF 转 `cjk16.h` 烧入。脚本=这道预生成工序，是原生渲染路线的硬性前提。

## 字库生成脚本落地（2026-07-13 晚）
- 已写 `firmware/tools/gen_cjk16.py`：从 `backend/fonts/truetype/NotoSerifSC-Regular.ttf` 渲染 16×16 1-bit 点阵，输出 `firmware/src/cjk16.h`（含 `cjk16_codepoints[]` + `cjk16_glyphs[][16]` + `cjk16_lookup()` 二分查找）。
- 运行环境：复用系统 Python 3.12（已带 Pillow 11.0.0）。managed venv 在 Windows 下创建异常（目录未生成），故直接用系统 python312 跑脚本，未新装包。
- 默认字表 `--level 1` = GB2312 一级汉字 3755 + 24 常用标点 = 3779 字；`--level 2` 扩到全量 6763。产物 .h 约 530KB 文本，编译后二进制约 118KB（3779×32B）。
- 验证：ASCII 自检 9 字（中文字库测试墨水屏）点阵清晰、bit15=左/黑=1/居中正确，与 drawGlyph16 取位 `(bits & (1<<(15-col)))` 一致；另生成 `firmware/tools/cjk16_preview.png` 192 字网格预览。
## 中英混排绘制原语落地（2026-07-13 晚）
- 在 `firmware/src/display.cpp` 内 `#include "cjk16.h"`，新增 `drawMixed()` / `measureMixed()` 及内部 helper：
  - `drawAsciiChar()`：复用 `getGlyph()` 5×7 列优先点阵，按 `scale` 缩放（默认 2→10×14）后写黑到 `imgBuf`。
  - `drawMissingGlyph()`：16×16 空心方框，用于 `cjk16_lookup()` 未命中的汉字。
  - `drawMixed(x,y,text,asciiScale=2)`：以 16×16 中文字格为基准，ASCII 底部对齐到字格底；UTF-8 解析（2/3 字节 CJK）→ 查 `cjk16_lookup()` → 调现有 `drawGlyph16()`；缺字回退方框。
  - `measureMixed(text,asciiScale)`：同规则计算像素宽度，供布局右对齐/居中。
- `display.h` 已声明 `drawMixed()` / `measureMixed()`，默认 `asciiScale=2`。
- 预览验证：`firmware/tools/render_preview.py` 解析 `cjk16.h` 真实点阵并复刻同一套取位规则，渲染完整 400×300 待办列表页（状态栏/待办区/底栏），输出 `firmware/tools/preview_todo.png`。目视确认：中文点阵清晰、中英混排（milk/review/call/Git/firmware/run/30）自然、符合黑白像素墨水风；程序化校验示例所有汉字均命中字库，无缺字方框。
- 状态栏时间当前用放大 ASCII 数字占位；后续需固件实现 7 段数码管大字库（里程碑 1 剩余项）。

## UI chrome 三层结构落地（2026-07-13 晚）
- `firmware/src/display.cpp` 在 `smartDisplay` 之后新增完整 UI chrome 层，全部写 `imgBuf` 后调 `smartDisplay`：
  - 布局宏：`UI_SB_H=52 / UI_FT_H=18 / UI_ROW_H=30 / UI_PER_PAGE=7`。
  - 7 段数码管时间 `draw7SegText`（`SEG_MASK[10]` + 段填充 a/g/d 横、f/b/e/c 竖，段厚 t=4、字宽 dw=22、字高 dh=40）→ 状态栏全局最大字号 HH:MM。
  - 图标：`drawClipboard`(清单) / `drawBattery(pct)`(电量) / `drawCheckbox(checked)`(待办勾选) / `drawMiniList`(底栏列表)。
  - `drawStatusBar(batteryPct)`：clipboard@(4,6) + 7seg 时间@(26,3) + 日期 `drawMixed(140,30,"%m/%d 星期X",2)` + 电池@(W-26,9) + 状态栏底线。
  - `drawTodoList(items,count)`：每行 checkbox@(8,...) + 正文 `drawMixed(30,...,2)`（按 `maxW=W-30-58-6` 截断防重叠）+ 提醒时间右对齐 + 行间虚线分隔（每 4px 一点）。
  - `drawFooter()`：底线 + miniList + "待办" + "— 诸事有序"（右对齐）。
  - `renderTodoScreen(items,count,page,totalPages,batteryPct)`：白底→状态栏→待办→底栏→分页→`smartDisplay`。
- `display.h` 新增 `struct TodoItem { const char *text; bool done; const char *remind; }` 与 `renderTodoScreen(...)` 声明。
- `config.h` 加 `#define INKSIGHT_TODO_DEMO 1`（mock 待办入口，后端接好后置 0）。
- `main.cpp` `setup()` 在 `showBootScreen()` 后插入 6 条 mock 待办 `renderTodoScreen(demo,6,0,3,87)` + 延时。

## Bug 修复：提醒时间/分页小字偏移（2026-07-13 晚，已闭合）
- 根因：`drawMixed` 对 ASCII 施加 `top = 16 - 7*asciiScale` 底部对齐偏移，纯 ASCII 的"提醒时间/分页"被下沉到待办行外/与底栏重叠（实测旧坐标统计黑像素=0）。
- 修复：固件新增 `drawAsciiText(x,y,text,scale)`（纯 ASCII、无 CJK 偏移，与预览 `render_ascii_text` 同构）；`drawTodoList` 提醒时间改用 `drawAsciiText(... remind,1)`，`renderTodoScreen` 分页改用 `drawAsciiText(... pg,1)` 且 y 对齐 `H - UI_FT_H - 14`。
  - 验证：`render_preview.py` 同构渲染 `preview_todo.png`，像素统计确认 6 行提醒时间黑像素 70~79、分页黑像素 29，均正常。底栏长破折号 U+2014 已在 `cjk16.h` 字库内（渲染为正常全角字，非缺字方框）。
- 里程碑 1 剩余：确认 `main.cpp` demo 入口真机/模拟器可编译运行；接 `inksight-content` 后端待办 CRUD（D2 决策）。

## 字体横线/粗细问题修复：中文改 fontmode=1 + 英文改半角 8x16（2026-07-13 晚）
- 现象：原 `gen_cjk16.py` 用 `Image.new("L")` 灰度 + 硬阈值 `<128`，导致细横笔（如"一、三、买"）在二值化时丢失；英文仍走 5×7 放大 2×，笔画比中文粗很多。
- 根因对照：后端 `backend/core/patterns/utils.py:apply_text_fontmode` 用 `draw.fontmode = "1"` 单色 hinting，与固件字库生成管线不同。数码管数字是实心色块，与字体管线无关，所以之前"数字和后端一样"不能推出整套方法可照搬。
- 修复方案 A（已落地）：
  1. `gen_cjk16.py` 中文 `render_glyph` 改为 `Image.new("1")` + `d.fontmode = "1"` 单色渲染；同脚本从同一 TTF 生成 8×16 半角 ASCII 字库（`ascii16_glyphs[95][16]`，0x20-0x7E，每字节一行，bit7=左）。
  2. 重新生成 `firmware/src/cjk16.h`：3779 中文字 + 95 ASCII，约 541KB。
  3. 固件 `display.cpp` 新增 `drawAscii16()`；`drawMixed` 与 `measureMixed` 的 ASCII 分支改为 `ascii16_lookup` + `drawAscii16` + advance 9px（8+1 gap）；CJK 保持 17px（16+1）。
  4. 预览脚本 `render_preview.py` 同步：解析 `ascii16_glyphs`，新增 `render_ascii16()`，`render_mixed`/`measure_mixed` 改走半角字库；保留 `render_ascii_text`（5×7×1）给提醒时间/分页。
- 验证：重新生成 `firmware/tools/preview_todo.png`，目视确认中文横笔（"一、三、牛奶、评审、三"）完整、ASCII（milk/review/call/Git/firmware/run/30）与中文粗细一致、不再过粗；字符画自检确认 `一` 为连续横线。总黑像素约 7308（合理范围）。
- 待办：用户本地 `pio run` 验证固件编译；确认真机显示效果。

## 编译告警修复：drawCheckbox 越界读（2026-07-13 晚）
- 现象：`drawCheckbox` 勾选点数组只有 13 个点，但循环写死 `k < 14`，第 14 次 `pts[13]` 越界 → `-Waggressive-loop-optimizations` undefined behavior 告警（链接仍能成功，侥幸）。
- 修复：循环上界改为 `int n = sizeof(pts)/sizeof(pts[0])`，按数组真实长度遍历，杜绝点数变动再踩坑。预览脚本 `render_preview.py::draw_checkbox` 本就用 `for ... in pts`（13 点），行为一直正确，无需改。
- 需用户在本地 `pio run` 复验告警消失。

## 时间显示修复：巨大 00:00（2026-07-13 晚）
- 现象：状态栏出现巨大 `00:00`。根因有二：① 7 段数码管被刻意设为状态栏最大字号（dw=22,dh=40,t=4，占 52px 状态栏的 40px 高）→ "巨大"是设计；② 显示读 `curHour/curMin` 全局变量（初值 0），`syncNTP()` 在 NTP 失败时把它们**强制清零为 0** → 没联网同步就永远 `00:00`；且 demo 渲染（main.cpp:575）在 `syncNTP()`（main.cpp:651）之前、之后未重绘。
- 用户要求：优先读本地 RTC → 取 NTP → 有差异用网络时间并写回本地。
- 修复：
  1. `network.cpp::syncNTP()` 重写：先 `time(nullptr)` 读本地 RTC；定义 `TIME_LOCAL_VALID_MIN=1700000000`（2023-11）区分"是否有效"（断电重置到 1970 视为无效）；`configTime` + `getLocalTime(5s)`；成功且本地无效→直接 `settimeofday` 用网络时间；成功且 `|tNet-tLocal|>1s`→写回网络时间；失败→**保留本地 RTC 不清零**（仅本地本就无效才退化为 00:00）。加 `#include <sys/time.h>`。
  2. `display.cpp::drawStatusBar()` 时间与日期统一改读 `time()` + `localtime_r`（直接取系统 RTC，被 NTP 写回后即时正确），不再依赖 `curHour/curMin`。`curHour` 仍被 `currentPeriodIndex()` 时段判断使用、由 syncNTP 维护，逻辑自洽。
- 物理限制提示：若设备从未联网同步过、且无电池备份 RTC，上电首次必显示 00:00（无时间源）；联网一次后即正确，断网也靠 RTC 维持。
- 时钟样式（用户决策）：选"改成普通数字"——删除 7 段数码管代码（SEG_MASK/draw7SegDigit/Colon/Text），`drawStatusBar` 时钟改走半角 `ascii16` 渲染 `HH:MM`（与正文同字号，`lineY=(UI_SB_H-16)/2` 垂直居中），日期同基线 `drawMixed(date,1)`；`segFill` 保留供图标用。编译通过（删码后 Flash 略降）。预览脚本 `render_preview.py::draw_status_bar` 同步为普通数字并重建 `preview_todo.png`。

## 新建独立后端服务 inksight-server（2026-07-13 深夜）
- 需求：项目根目录新建独立后端，提供两类接口——① 待办（对接滴答清单 Dida365 API）② 图片资源拉取（读站点 images/ 目录，图片由配套脚本后续生成，本期仅目录规划+端口配置）。密钥/参数统一放站点根目录 `.env`；旧 `backend/` 待新服务上线稳定后再移除（**本期未删**）。
- 落点：`inksight-server/`（FastAPI）。结构：`app/{main.py,config.py,dida/{client.py,auth.py},routers/{todos.py,images.py},web/templates/index.html}`、`images/`（资源目录+README）、`.env.example`、`.gitignore`、`requirements.txt`、`README.md`。
- 接口契约：
  - `GET /api/todos?mac=` → `{updated_at,source:"dida365",items:[{id,text,done,remind_at,due,project}]}`；未授权→503 引导 `/admin/dida365/auth`；可选 `X-Device-Secret`（设 `DEVICE_SHARED_SECRET` 时校验）。
  - `GET /api/images/manifest` → `{version(文件名+mtime 的 sha1),count,images:[{slot,name,url}]}`；`GET /api/images/{name}` → 文件（防目录遍历）。
  - `GET /admin/dida365/auth` 跳转授权、`/callback` 用 code 换令牌存 `.dida_tokens.json`。
- Dida365 关键信息（已 WebSearch 核实）：OAuth authorize/token 在 `https://dida365.com/oauth`，API base `https://api.dida365.com/open/v1`，Bearer 令牌；任务可靠获取方式是用 `GET /project/{id}/data`（非 `/task`，后者易 500）；默认 scope `tasks:read`。已在 ima KB `esp32-rlcd-4.2` 存有 TickTick Developer 链接（developer.dida365.com）作参考。
- 冒烟测试：用受管 venv（`C:/Users/liuyz/.workbuddy/binaries/python/envs/inksight-server`）装 fastapi/uvicorn/httpx/python-dotenv；`py_compile` + `import app.main` 通过；uvicorn 起服务实测 `/health`=200、`/api/images/manifest` 空目录返回合法 version、`/api/todos` 未授权=503、图片文件 200、缺失 404、遍历被拒。已停测试服务。
- 设备默认显示待办（固件 `TODO` 默认态），按键切图片页——属固件侧，本期后端只提供两类数据；`config.h` 的 server 地址后续接本服务。
- 待办：用户填 `.env` 的 Dida365 凭据并访问 `/admin/dida365/auth` 完成授权；真机联调 `/api/todos` 与图片 manifest；图片生成脚本（样式规则）本期未做。

## 固件接入新后端 inksight-server（2026-07-13 深夜，v2 迁移）
- 用户确认：旧 inksight 后端网址已停用，改用 `https://esp32.122050.xyz` 作为唯一后端（固件 + 云服务器都跑这个域名）。
- `config.h`：新增 `DEFAULT_SERVER="https://esp32.122050.xyz"`、`#define BACKEND_TLS_INSECURE 1`（反代 LE 证书不匹配硬编码 ROOT_CA，故 `beginHttpForUrl` 改用 `secClient.setInsecure()`）、`#define INKSIGHT_BACKEND_V2 1`、`#define TODO_MAX 24`。
- `platformio.ini`：新增 `lib_deps = bblanchon/ArduinoJson@^7.2.0`（用户批准可装；v7 用 `JsonDocument doc;` 不带容量参数，旧的 `DynamicJsonDocument(cap)` 已弃用且报错）。
- `network.cpp`：
  - `beginHttpForUrl` 支持 insecure TLS。
  - 旧后端设备管理等函数（postHeartbeat/fetchFocusListeningFlag/fetchFocusAlertBMP/hasPendingRemoteAction/peekPendingMode/postConfigToBackend/postRuntimeMode/ensureDeviceToken）在 `INKSIGHT_BACKEND_V2` 下全部 no-op（返回安全默认值），不再打旧端点。
  - 新增 `fetchTodos(TodoItem*,int&,int)`：GET `/api/todos`，ArduinoJson 解析 `items[]` 的 text/done/remind_at(或 due→HH:MM)；用 file-static 缓冲 `g_todoText/g_todoRem/g_todoDone` 保证指针稳定。
  - 新增 `fetchImageManifest`(version,names[],count,max) 与 `fetchImageByName`(name)（复用抽出的 `decodeBmpToImgBuf` 把 BMP 解到 imgBuf）。
  - `fetchBMP` 改为 `#if INKSIGHT_BACKEND_V2`：manifest→按 `g_imgIdx`(static) 取图（nextMode 前进一位循环）；`#else` 保留旧 `/api/render` 路径。
- `main.cpp`：
  - 启动段（原"Fetching image"）改为：连 WiFi 后 `fetchTodos` → 成功 `renderTodoScreen` 渲染真待办，失败保留 demo 列表；**保持 WiFi 在线**（不再断网），`g_view=TODO`。
  - 新增 `enum AppView{TODO,IMAGE}` + `static AppView g_view`：默认 TODO；短按 KEY→切到 IMAGE 并拉首图，IMAGE 下短按→下一张。
  - `loop()` 周期刷新视图感知：TODO 视图重拉 `fetchTodos` 并仅在校验和变化时 `smartDisplay`；IMAGE 视图走 `triggerImmediateRefresh`。
- 编译通过（Flash 22.8%/RAM 24.8%）。注意：旧后端的 heartbeat/state/ota 等调用在 v2 下是 no-op，不影响运行；`ota.cpp` 的 `ensureDeviceToken` 在 v2 下返回 true，OTA 走旧端点会失败（旧后端已弃用，OTA 暂不可用，待后续评估）。
- 需求 #4（移除旧 backend）进度：固件侧已完成"切换数据源"；旧 `backend/` 目录尚未删除（按用户"新后端上线后再移除"），待真机联调通过后再清理。

## 七项 UI/时间修复（2026-07-13 更晚）
问题来源：用户部署 `inksight-server` 到 `esp32.122050.xyz` 后真机出现 00:00、状态栏前面有方框、底栏太矮、待办无分页等 7 项反馈。
修复内容：
- 时间 00:00：`network.cpp::syncNTP()` 改为只初始化一次 SNTP、重试 3 次共约 15s；新增 `loop()` 周期性 NTP 重同步（RTC 无效时 30s 一次，有效后 5min 一次），RTC 从无效变有效时自动重绘当前视图。
- 状态栏前面方框：删除 `drawClipboard` 图标及调用，时间从 x=4 开始。
- 底栏加高：`UI_FT_H` 18→26，内容垂直居中。
- 底栏图标与"待办"间距：x 16→24。
- 待办行数固定 6 条：`display.cpp` 和 `render_preview.py` 的 `UI_PER_PAGE` 7→6；`main.cpp` 新增 `TODO_PER_PAGE`+`g_todoPage`+`g_todoTotalPages`，按当前页切片渲染，分页提示为 `page/total`。
- WiFi 图标：状态栏右侧新增 `GLYPH_WIFI`（16×16），仅当 `WiFi.status()==WL_CONNECTED` 时绘制，位于电量文字左侧。
- 电量改文字：移除电池图标 `drawBattery`，改为 "电量:xx%"（使用 `drawMixed` 半角 ASCII，% 在 ascii16 字库范围内）。
- 按键模型调整：短按 KEY 在 TODO 视图先翻页；单页（≤6 条）时直接切到 IMAGE；翻到最后一页后下一次短按切 IMAGE；IMAGE 下短按仍切下一张图片。
- `renderTodoScreen()` 新增 `wifiConnected` 和 `autoDisplay` 参数，避免 TODO 周期刷新重复刷屏；预览脚本同步，preview_todo.png 已重生成。
- 编译通过（Flash 22.8%/RAM 24.8%）。

